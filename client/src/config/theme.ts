import { createMuiTheme } from "@material-ui/core/styles";

export const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#718792",
      main: "#455a64",
      dark: "#1c313a",
    },
    secondary: {
      light: "#ff616f",
      main: "#ff1744",
      dark: "#c4001d",
    },
  },
});
